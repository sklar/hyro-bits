# Select

## ✨ Import

```typescript
import { 
  Select, 
  SelectProps,

  AsyncSelect,
  AsyncSelectProps,

  CustomSelectProps,

  // re-exported from `react-select`
  ReactSelectComponents,
  ReactSelectGroupBase,
  ReactSelectProps,

  // re-exported from `react-select/async`
  ReactAsyncSelectProps,

  // styles
  components,
  container,
  control,
  indicator,
  indicatorsContainer,
  input,
  loadingIndicator,
  menu,
  menuList,
  message,
  multiValue,
  placeholder,
  singleValue,
  valueContainer
} from "purplex-components/base/select";
```
