export { B as Button, a as ButtonProps } from '../../button-bd8fafa7';
import { SerializedStyles } from '@emotion/react';
import 'react';
import '../../index-2e22dac1';

declare const button: SerializedStyles;
/**
 * Note that [data-hover] and [data-pressed] are here only to help
 * better illustrate `:hover` and `:active` state in the dedicated story.
 */
declare type Variants = Record<string, SerializedStyles>;
/**
 * Button sizes
 */
declare const sizes: Variants;
declare const variants: Variants;

export { button, sizes, variants };
